use crate::error::Result;

/// Handle `slate status` command
pub fn handle(_args: &[&str]) -> Result<()> {
    // will implement full status display
    println!("Status display — implemented in ");
    Ok(())
}
